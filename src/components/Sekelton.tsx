const Sekelton = ({ className }: { className?: string }) => {
  return (
    <div
      className={`p-4 rounded-lg shadow animate-pulse bg-gray-200 h-52 ${className}`}
    />
  );
};

export default Sekelton;
